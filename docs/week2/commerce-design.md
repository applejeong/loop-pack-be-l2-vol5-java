
## 1.버드뷰
고객 - 상품,브랜드 조회 / 좋아요 / 포인트 / 주문   ─┐
                                           ├─commerce-api - db
관리자 - 상품,브랜드 CRUD / 재고 변경 / 주문 조회 ─┘ 

## 2.구조와 의존
|계층|역할|의존금지|
|--|--|--|
|interfaces| HTTP 요청/응답 처리 | infrastructure
|application| 유스케이스 조율 | controller DTO, infrastructure 구현 |
|domain|상태·규칙, repository 인터페이스 | interfaces, application, infrastructure |
|infrastructure|repository의 JPA 구현 | - |

## 3.도메인 관계
| 관계                          | 다중성   | 연결 방식                                              | 필요한 이유                                                                                | 책임 주체                                                                          |
|-----------------------------|-------|----------------------------------------------------|---------------------------------------------------------------------------------------|--------------------------------------------------------------------------------|
| Brand - Product             | 1:N   | 직접(Product가 brandId보유)                             | 브랜드 삭제 시 연결 상품 존재 확인                                                                  | Product                                                                        |
| User - Like - Product       | 1:N:1 | 간접(좋아요 테이블에 User와 Product가 각각 존재)                  | 유저당 상품에 좋아요를 1번만 누를 수 있기 때문(중복방지)                                                     | Like(동일 User-Product 조합 저장을 막는 규칙을 Like 저장 시점에 검증)                             |
| Order - OrderItem - Product | 1:N:1 | 간접                                                 | 한 주문에 여러 상품을 가질 수 있어야함(Order-OrderItem), 주문 시점의 금액과 수량을 독립적으로 기록해야함(OrderItem-Product) | OrderItem(주문할 때 상품의 수량,단가 결정), Product(재고 수량 체크),Order(여러 OrderItem을 묶어 상태 관리) 
| User - Point                | 1:1   | 직접 (User가 잔액 값을 속성으로 가짐, 또는 Point라는 별도 Entity로 분리) | 유저가 취급하는 포인트는 하나이기 때문, 충전이나 차감 시 유효성(0원 이상, 오버플로우 방지)을 지켜야 함                          | Point(포인트 충전과 차감)                                                              
| User - Order | 1:N | 직접 (Order가 userId 보유) | 유저가 여러 건의 주문을 만들 수 있음, 본인 주문만 조회·확정 가능해야 함 | Order(자신의 상태·금액 규칙 관리) + application(본인 소유 여부 검증, 요청자-userId 일치 확인) |


## 4.API 계약 (입력/성공/오류) - method/path/입력/성공/대표 오류와 주요 규칙의 기대값
### 4-1.고객

| Method/Path | 입력 | 성공 결과 | 대표 오류 | 주요 규칙 기대값 |
|---|---|---|---|---|
| GET /api/v1/brands/{brandId} | brandId(path) | 200, 브랜드 상세 | 없거나 삭제된 브랜드 → 404 | 삭제된 브랜드는 조회 불가 |
| GET /api/v1/products | brandId(query, optional), sort(latest/price_asc/likes_desc), page | 200, 상품 목록(브랜드정보+좋아요수 포함) | 잘못된 정렬값 → 400 | 동률 시 id 등 보조 정렬 기준 적용, 삭제된 상품은 목록 제외 |
| GET /api/v1/products/{productId} | productId(path) | 200, 상품 상세(브랜드정보+좋아요수 포함) | 없거나 삭제된 상품 → 404 | - |

#### 4-2.관리자

| Method/Path | 입력 | 성공 결과 | 대표 오류 | 주요 규칙 기대값 |
|---|---|---|---|---|
| GET /api-admin/v1/brands | page(query) | 200, 브랜드 목록 | 권한 없음 → 403 | 관리자만 접근 가능 |
| POST /api-admin/v1/brands | name 등 브랜드 정보 | 201, 등록된 브랜드 | 정보 유효성 실패 → 400, 권한 없음 → 403 | 이름 등 필수값 검증 |
| GET /api-admin/v1/brands/{brandId} | brandId(path) | 200, 브랜드 상세 | 없음 → 404, 권한 없음 → 403 | - |
| PUT /api-admin/v1/brands/{brandId} | brandId(path), 수정할 정보 | 200, 수정된 브랜드 | 없음 → 404, 유효성 실패 → 400 | - |
| DELETE /api-admin/v1/brands/{brandId} | brandId(path) | 204 | 삭제되지 않은 연결 상품 존재(재고0 포함) → 409, 없음 → 404 | 연결 상품이 하나라도 남아있으면 거절 |
| GET /api-admin/v1/products | brandId(query, optional), page | 200, 상품 목록 | 권한 없음 → 403 | - |
| POST /api-admin/v1/products | brandId, name, price 등 | 201, 등록된 상품 | 브랜드 없음/삭제됨 → 404, 이름·가격 범위 실패 → 400 | 브랜드 존재·미삭제 확인 |
| GET /api-admin/v1/products/{productId} | productId(path) | 200, 상품 상세 | 없음 → 404 | - |
| PUT /api-admin/v1/products/{productId} | productId(path), 수정할 정보 | 200, 수정된 상품 | 없음 → 404, 유효성 실패 → 400 | 수정 시 브랜드는 유지(변경 불가) |
| DELETE /api-admin/v1/products/{productId} | productId(path) | 204 | 없음 → 404 | 삭제 후 고객 조회·새 주문·새 좋아요에서 제외 |
| PUT /api-admin/v1/products/{productId}/stock | productId(path), quantity | 200, 변경된 재고 | 음수 → 400, 없음 → 404 | 0 이상인 최종 수량으로 설정(증감이 아니라 절대값) |

---

### 4-3.좋아요

| Method/Path | 입력 | 성공 결과 | 대표 오류 | 주요 규칙 기대값 |
|---|---|---|---|---|
| POST /api/v1/products/{productId}/likes | productId(path), X-USER-ID(header) | 201, 등록됨 | 식별 누락/없는 사용자 → 401, 없거나 삭제된 상품 → 404 | 동일 User-Product 조합은 1개까지만 저장(중복 방지, 재등록 시도 시 멱등 처리 또는 무시) |
| DELETE /api/v1/products/{productId}/likes | productId(path), X-USER-ID(header) | 204, 취소됨 | 식별 누락/없는 사용자 → 401, 본인 관계 아님/없음 → 404 | 삭제된 상품이어도 기존 좋아요는 취소 가능 |
| GET /api/v1/users/{userId}/likes | userId(path), X-USER-ID(header) | 200, 내 좋아요 목록 | 식별 누락 → 401, 본인 아님 → 403 | 삭제된 상품은 목록에서 제외, 각 상품의 좋아요 수 포함 |

---

### 4-4.주문

| Method/Path | 입력 | 성공 결과 | 대표 오류 | 주요 규칙 기대값 |
|---|---|---|---|---|
| POST /api/v1/orders | items[{productId, quantity}], X-USER-ID(header) | 201, DRAFT 상태 주문(품목·수량·단가·합계 포함) | 식별 누락 → 401, 상품 없음/삭제됨 → 404, 수량 0 이하 → 400 | 생성 시점엔 재고 차감 없음, 중복 상품은 합산 또는 거절(정책 결정 필요) |
| POST /api/v1/orders/{orderId}/confirm | orderId(path), X-USER-ID(header) | 200, CONFIRMED 상태 주문(결제액 포함) | 본인 주문 아님 → 403, DRAFT 아님(이미확정) → 409, 재고부족 → 409, 포인트부족 → 409 | 확정 시에만 재고·포인트 차감(재고→포인트→확정 순서), 실패 시 상태 변경 없음(롤백) |
| GET /api/v1/orders | X-USER-ID(header) | 200, 내 주문 목록 | 식별 누락 → 401 | 본인 주문만 조회 |
| GET /api/v1/orders/{orderId} | orderId(path), X-USER-ID(header) | 200, 주문 상세(품목·수량·금액·상태·결제액) | 본인 아님/없음 → 403/404 | 본인 주문만 조회 |

### 4-5.관리자 (주문)

| Method/Path | 입력 | 성공 결과 | 대표 오류 | 주요 규칙 기대값 |
|---|---|---|---|---|
| GET /api-admin/v1/orders | page(query) | 200, 전체 주문 목록(구매자별) | 권한 없음 → 403 | - |
| GET /api-admin/v1/orders/{orderId} | orderId(path) | 200, 주문 상세(품목·상태·금액·결제결과) | 권한 없음 → 403, 없음 → 404 | - |


## 5.대표 흐름
### 5-1. 관리자 변경 → 고객 조회
* 근거 문장 "관리자 변경을 고객 조회에 반영하고, 고객·관리자에게 제공할 응답 필드를 구분합니다."

#### 5-1-1. 상품 가격 수정
* 관리자가 상품 가격을 4,000원 → 5,000원으로 수정 요청
→ PUT /api-admin/v1/products/{productId}
→ 관리자 권한 확인 → 가격 유효성 검증 → Product.changePrice(5000) 호출
→ 저장됨

* 고객이 이 상품을 상세 조회
→ GET /api/v1/products/{productId}
→ Product 조회 → 응답에 가격 5,000원이 반영되어 보임

#### 5-1-2. 상품 삭제
* 관리자가 상품을 삭제 요청
→ DELETE /api-admin/v1/products/{productId}
→ 관리자 권한 확인
→ 삭제 처리 

* 고객이 상품 목록을 조회
→ GET /api/v1/products
→ 삭제된 상품은 목록에서 제외되어 응답
→ 새 좋아요 등록 불가  (기존 좋아요는 취소 가능)
→ 새 주문에서 제외

#### 5-1-3. 재고 변경
* 관리자가 재고를 0으로 변경
→ PUT /api-admin/v1/products/{productId}/stock
→ 관리자 권한 확인 → 0 이상인지 검증 → 최종 수량으로 설정

* 고객이 상품 상세 조회 → 재고 0인 상태로 보임

#### 5-1-4. 상품 등록
* 관리자가 새 상품을 등록
→ POST /api-admin/v1/products
→ 관리자 권한 확인
→ 브랜드 존재/미삭제 확인 → 이름·가격 유효성 검증 → 저장

* 고객이 상품 목록을 조회
→ GET /api/v1/products
→ 방금 등록된 상품이 목록에 나타남

### 5-2. 좋아요 등록 · 취소
#### 5-2-1. 고객이 상품 좋아요를 등록
→ POST /api/v1/products/{productId}/likes
→ 삭제된 상품인지 확인
→ 사용자-상품관계 유니크 체크 (상품이 없거나(404) 좋아요가 이미 등록되어있는 경우(401) 거절)
→ 좋아요가 등록됨

#### 5-2-2. 고객이 상품 좋아요를 취소
→ DELETE /api/v1/products/{productId}/likes
→ 사용자 식별 누락/존재하지 않는 상품(401) 요청 거절
→ 본인의 좋아요만 취소 가능(404)
→ 좋아요가 취소됨

### 5-3. 포인트 충전 → 주문 확정
#### 5-3-1. 고객이 포인트를 충전한다.
→ POST /api/v1/points/charge
→ 누락, 잘못된 타입, 표현 범위 초과를 거절
→ 오류 입력 시 기존 잔액 유지
→ 검증 통과 시: 포인트가 충전됨

#### 5-3-2. 고객이 주문을 생성한다.
→ POST /api/v1/orders
→ 상품이 존재하며 삭제되지 않았는지, 수량이 양수인지 확인
→ 생성 시점에는 재고 차감이 없음 (DRAFT 상태로 저장)

#### 5-3-3. 고객이 주문을 확정한다.
→ POST /api/v1/orders/{orderId}/confirm
→ 본인의 주문만 확정 가능
→ 상품 재고 확인 → 부족하면 거절 (DRAFT 유지, 아무것도 차감 안 됨)
→ 재고 충분하면 차감 → 포인트 잔액 확인 → 부족하면 거절
→ 여러 품목 주문시 중복 상품은 합산하거나 거절됨 (총수량 기준 재고 확인)
→ 재고가 차감됨
→ 포인트가 차감됨
→ 주문이 확정됨(CONFIRMED), 결제액 저장

## 6. AI와 설계 다듬기
AI와 설계 다듬기

### 6-1. OrderItem 필요 근거 검증
- 질문: "요구사항에 가격이 바뀐다는 얘기는 없었잖아. 바뀔 수도 있다는 건
  너의 생각 아니야?"
- 검토: AI가 "가격이 나중에 바뀔 수 있어서 unitPrice가 필요하다"고 설명했으나,
  이는 과제 원문에 없는 일반론적 추론이었음을 확인
- 결론: unitPrice 저장 근거를 과제 원문에 명시된 "여러 품목·수량·단가·합계를
  저장한다"는 요구사항 자체로 재정리. AI의 일반론과 실제 요구사항을 구분함.

### 6-2. 관리자 변경→고객 조회 흐름의 범위 판단
- 질문: (브랜드 수정/삭제/등록도 고객에게 반영되는지 설명을 들은 뒤)
  "그렇다면 굳이 안 적어도 되는 거 아니야?"
- 검토: 이미 상품 CRUD(가격수정/삭제/재고변경/등록) 4개 예시로
  "관리자 변경→고객 조회" 패턴을 충분히 보여줬다고 판단
- 결론: 브랜드 관련 흐름은 추가하지 않고 상품 4개 예시로 충분하다고 결정.
  같은 패턴의 반복 추가는 비용 대비 효용이 낮다고 판단.

### 6-3. User-Product 사이의 연결 경로 재검토
- 질문: "유저와 프로덕트 사이에는 주문도 낄 수 있지 않아?"
- 검토: Like(User-Product 직접 매개, 1:1 단순 관계)와
  Order-OrderItem-Product(2단계 매개, 여러 품목을 묶는 복합 관계)가
  서로 다른 목적과 다중성을 가진 별개의 연결임을 확인
- 결론: 두 관계를 도메인 관계 표에 각각 별도 행으로 분리해서 기록

### 6-4. 이벤트스토밍 커맨드/이벤트 색상 구분 오류 반복 검증
- 질문: (여러 이벤트스토밍 이미지를 제시하며) "이렇게 가면 되나?",
  "화살표는 아래로 향해야 되는거야?" 등
- 검토: 포인트/주문/좋아요/상품/브랜드 다이어그램 전반에서
  "~한다"(명령형) 문장이 주황(이벤트) 색으로 잘못 표시된 경우가
  반복적으로 발견됨
- 결론: 커맨드(파랑, 명령형)와 이벤트(주황, 과거형)를 색과 문장 형태
  기준으로 재분리. 조회 결과는 이벤트가 아니라 읽기모델(초록)로 정정.

### 6-5. 대표 흐름 작성 중 빠뜨린 조건 재검토
- 질문: (포인트충전→주문확정 흐름 초안을 제시하며) "이런식으로 가야되나?
  빼먹은 게 있으면 추가해줘"
- 검토: "삭제되지 않은 브랜드만 주문 생성 가능"이라는 조건이 과제 원문에
  없고, 실제로는 "삭제되지 않은 상품"이 검증 대상임을 확인
- 결론: 브랜드 관련 조건을 상품 관련 조건으로 정정, 확정 실패 시
  주문 상태가 DRAFT로 유지된다는 내용 추가